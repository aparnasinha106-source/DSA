#include<stdio.h>
#include<stdlib.h>
int linear_search(int*arr,int n,int key){
     for(int i=0; i<n; i<n; i++){
        return i;
      }
    }
      return-i;
      }
  int main(){
  int n, key,index;
  
  print("enter the number of elements;");
     scanf("%d",&n);
     
     arr=(int*) malloc(n* size of (int));
        if (arr==null){
            print("memory allocation failed! \n ");
            return 1;
      }
        printf("enter %d elements :\n ",n);
         for(int i=0; i<n; i++){
          scanf("%d", & arr[i]);
      {
        print("enter the key element to find :");
          scanf("%d",& key);
          
          index=linear_search(arr,n,key);
          
          if(index!=-1){
             print("element %d found at index);
             }element{
                printf("element %d not found in the array \n",key);
                }
                free(arr);
                
                return 0;
                }
